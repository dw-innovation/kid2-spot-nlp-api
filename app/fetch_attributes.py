import os
import pandas as pd
# db = client[os.getenv("TAG_DB")]
# pd.read_csv()
#
# core_tags = {}
# for tag in tag_df.to_dict(orient='records'):
#     all_tags[int(tag['index'])] = tag
#     descriptors = tag['descriptors'].split('|')
#
#     if tag['type'] == 'core':
#         core_tags[int(tag['index'])] = tag